<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:drawable="@color/blackGrey" android:state_pressed="true"/>
    <!-- Default state -->
    <item android:drawable="@color/colorDefault"/>
</selector>