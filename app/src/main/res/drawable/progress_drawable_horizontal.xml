<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background -->
    <item android:id="@android:id/background">
        <shape
            android:shape="rectangle">
            <corners android:radius="@dimen/_10sdp" /> <!-- Bo tròn góc -->
            <solid android:color="@color/gray_light" /> <!-- Màu nền -->
        </shape>
    </item>
    <!-- Progress -->
    <item android:id="@android:id/progress">
        <clip>
            <shape
                android:shape="rectangle">
                <corners android:radius="@dimen/_10sdp" /> <!-- Bo tròn góc -->
                <gradient
                    android:angle="0"
                    android:startColor="@color/colorRedLight"
                    android:endColor="@color/colorRedM"
                    android:type="linear" /> <!-- Gradient màu -->
            </shape>
        </clip>
    </item>
</layer-list>
